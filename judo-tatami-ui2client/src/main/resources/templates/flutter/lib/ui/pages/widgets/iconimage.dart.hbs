{{ log 'Icon generation for' (variable application.name) '-' (fqClass page.name)'page' label level='debug' ~}}
//START iconimage.dart.hbs-----{{page.name}}-----------------------------------------------------------------------------------------------
JudoIcon(
    col: {{ col }},
    row: {{ row }},
    icon: getIconGuessFavorMaterial(name: "{{ icon.name }}") ?? Icon(MdiIcons.fromString("{{ icon.name }}")),
),
//END iconimage.dart.hbs-----{{page.name}}-----------------------------------------------------------------------------------------------
