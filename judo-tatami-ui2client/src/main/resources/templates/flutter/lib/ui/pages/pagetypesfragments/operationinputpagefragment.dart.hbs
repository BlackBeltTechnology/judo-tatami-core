{{# if addDebugToTemplate }}//START operationinputpagefragment.dart.hbs-----{{ page.name }}------------------------------------------------------------------------------{{/if}} if }

    final Function operationCall;

    const {{ pageClassName page }}({Key key, @required this.operationCall}) : super(key: key);

    @override
    _{{ pageStateClassName page }} createState() => _{{ pageStateClassName page }}(operationCall: operationCall);
}

class _{{ pageStateClassName page }} extends State<{{ pageClassName page }}> {

    final {{ storeClassName page.dataElement.target }} targetStore = {{ storeClassName page.dataElement.target }}();

    final {{ pageStoreClassName page }} pageStore = {{ pageStoreClassName page }}();

    final Function operationCall;

    _{{ pageStateClassName page }}({@required this.operationCall}){
        pageStore.targetStore = targetStore;
        pageStore.operationCall = operationCall;

        final navigation = locator<NavigationState>();
        SchedulerBinding.instance.addPostFrameCallback((_) {
            navigation.setCurrentTitle(AppLocalizations.of(context).lookUpValue(context, {{ pageClassName page }}.title));
        });
    }
    {{# if addDebugToTemplate }}//END operationinputpagefragment.dart.hbs-----{{ page.name }}--------------------------------------------------------------------------------{{/ if }}
