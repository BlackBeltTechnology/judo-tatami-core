await judoDialog<{{ fqClass page.dataElement.target.name }}Store>(
    context: context,
    selectableList: await {{fqVariable page.name}}PageStore.getRangeFor{{ fqClass page.relationType.name }}({{ variable page.dataElement.owner.name }}Store),
    toStringFunction: (element) => element.{{page.dataElement.target.representation.name}}.toString(),
);
