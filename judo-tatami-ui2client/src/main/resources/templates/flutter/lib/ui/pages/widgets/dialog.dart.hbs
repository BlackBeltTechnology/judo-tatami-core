{{ log 'Select dialog generation for' (variable application.name) '-' (fqClass page.name)'page' (fqClass container.layoutType.name) (variable relation.name) level='debug' ~}}
{{# if addDebugToTemplate }}//START dialog.dart.hbs-----{{ relation.name }}----------------------------------------------------------------------------------------------{{/ if }}
await judoSelect{{ dialogClassName page relation.name }}(
    context: context,
    ownerStore: {{~# if page.isPageTypeTable ~}}{{~# unless page.dataElement.isAccess ~}}owner{{ else }}target{{/ unless }}{{ else }}target{{/ if }}Store,
    pageStore: pageStore,
    {{# if addDialog }}
    dialogTypeAdder: true,
    {{/ if }}
);
{{# if addDebugToTemplate }}//END dialog.dart.hbs-----{{ relation.name }}----------------------------------------------------------------------------------------------{{/ if }}
