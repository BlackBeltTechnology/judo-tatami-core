await judoDialog<{{ fqClass page.dataElement.owner.name }}Store>(
    context: context,
    selectableList: await {{fqVariable page.name}}PageStore.getRange{{ fqClass page.dataElement.owner.name }}({{ variable page.dataElement.owner.name }}Store),
    toStringFunction: (element) => element.{{page.dataElement.target.representation}}.toString(), // TODO: check this line
);
