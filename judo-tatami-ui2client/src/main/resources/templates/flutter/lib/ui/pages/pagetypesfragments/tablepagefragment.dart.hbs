//START tablepagefragment.dart.hbs-----{{ page.name }}---------------------------------------------------------------------------------------
{{# with page.relationType as |relation| }}
    {{# unless page.dataElement.isAccess }}
    final {{ storeClassName page.dataElement.owner.name }} ownerStore;
    {{/ unless }}

    {{# unless page.dataElement.isAccess }}
    const {{ pageClassName page.name }}({Key key, @required this.ownerStore}) : super(key: key);
    {{/ unless }}

    @override
    State<StatefulWidget> createState() => _{{ pageStateClassName page.name }}({{# unless page.dataElement.isAccess }}ownerStore: ownerStore{{/ unless }});
}

class _{{ pageStateClassName page.name }} extends State<{{ pageClassName page.name }}> {
    {{# unless page.dataElement.isAccess }}
    final {{ storeClassName page.dataElement.owner.name }} ownerStore;
    {{/ unless }}

    final {{ pageStoreClassName page.name }} pageStore = {{ pageStoreClassName page.name }}();

    _{{ pageStateClassName page.name }}({{# unless page.dataElement.isAccess }}{@required this.ownerStore}{{/ unless }}){
        final navigation = locator<NavigationState>();
        SchedulerBinding.instance.addPostFrameCallback((_) {
            navigation.setCurrentTitle(AppLocalizations.of(context).lookUpValue(context, {{ pageClassName page.name }}.title));
        });

        pageStore.get{{ fqClass page.dataElement.name }}({{# unless page.dataElement.isAccess }} ownerStore {{/ unless }}).catchError((error) => ErrorHandler.showErrorSnack(context, error.message));
    }
{{/ with }}
//END tablepagefragment.dart.hbs-----{{ page.name }}-----------------------------------------------------------------------------------------
