{{ log 'Link formatter text generation for' (variable application.name) '-' (fqClass link.name)'link' (variable dataName) (variable part.attributeType.name) (variable parameterVariable) level='debug' ~}}
{{# if (isEnumType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? {{ parameterVariable }}.{{ part.attributeType.name }}.toString().split('.').last : '< None >'
{{ else }}
{{#if (isTimestampType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd HH:mm').format({{ parameterVariable }}.{{ part.attributeType.name }}) : '< None >'
{{else}}
{{#if (isDateType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd').format({{ parameterVariable }}.{{ part.attributeType.name }}) : '< None >'
{{else}}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? {{ parameterVariable }}.{{ part.attributeType.name }}.toString() : '< None >'
{{/if}}
{{/if}}
{{/ if }}
