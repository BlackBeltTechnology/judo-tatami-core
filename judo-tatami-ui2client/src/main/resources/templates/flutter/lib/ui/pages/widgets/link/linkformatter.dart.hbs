{{ log 'Link formatter text generation for' (variable application.name) '-' (fqClass link.name)'link' (variable dataName) (variable part.attributeType.name) (variable parameterVariable) level='debug' ~}}
//START linkformatter.dart.hbs-----{{dataName}}--------------------------------------------------------------------------------------------
{{# if (isEnumType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? {{ parameterVariable }}.{{ part.attributeType.name }}.toString().split('.').last : ''
{{ else }}
{{#if (isTimestampType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd HH:mm').format({{ parameterVariable }}.{{ part.attributeType.name }}) : ''
{{else}}
{{#if (isDateType part.attributeType.dataType) }}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd').format({{ parameterVariable }}.{{ part.attributeType.name }}) : ''
{{else}}
{{ parameterVariable }}.{{ part.attributeType.name }} != null ? {{ parameterVariable }}.{{ part.attributeType.name }}.toString() : ''
{{/if}}
{{/if}}
{{/ if }}
//END linkformatter.dart.hbs-----{{dataName}}----------------------------------------------------------------------------------------------
