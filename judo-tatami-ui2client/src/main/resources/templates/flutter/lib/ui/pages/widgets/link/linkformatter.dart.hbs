{{ log 'Link formatter text generation for' (variable application.name) '-' (fqClass link.name)'link' (variable dataName) (variable part.attributeType.name) (variable parameterVariable) level='debug' ~}}
{{# if addDebugToTemplate}}//START linkformatter.dart.hbs-----{{ dataName }}--------------------------------------------------------------------------------------------{{/ if }}
{{# if (isEnumType part.attributeType.dataType) }}
{{ variable parameterVariable }}.{{ variable part.attributeType.name }} != null ? {{ variable parameterVariable }}.{{ variable part.attributeType.name }}.toString().split('.').last : ''
{{ else }}
{{# if (isTimestampType part.attributeType.dataType) }}
{{ variable parameterVariable }}.{{ variable part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd HH:mm').format({{ variable parameterVariable }}.{{ variable part.attributeType.name }}) : ''
{{ else }}
{{# if (isDateType part.attributeType.dataType) }}
{{ variable parameterVariable }}.{{ variable part.attributeType.name }} != null ? DateFormat('yyyy-MM-dd').format({{ variable parameterVariable }}.{{ variable part.attributeType.name }}) : ''
{{ else }}
{{ variable parameterVariable }}.{{ variable part.attributeType.name }} != null ? {{ variable parameterVariable }}.{{ variable part.attributeType.name }}.toString() : ''
{{/ if }}
{{/ if }}
{{/ if }}
{{# if addDebugToTemplate}}//END linkformatter.dart.hbs-----{{ dataName }}----------------------------------------------------------------------------------------------{{/ if }}
