//START editpagefragment.dart.hbs-----{{ page.name }}----------------------------------------------------------------------------------------
    final {{ storeClassName page.dataElement.target.name }} targetStore;
    {{# if (validatableFlagNeed page.dataElement) }}
    final bool validatable;
    {{/ if }}

    const {{ pageClassName page.name }}({Key key, @required this.targetStore{{# if (validatableFlagNeed page.dataElement) }}, this.validatable{{/ if }} }) : super(key: key);

    @override
    State<StatefulWidget> createState() =>
        _{{ pageStateClassName page.name }}(targetStore: targetStore{{# if (validatableFlagNeed page.dataElement) }}, validatable: validatable{{/ if }});
}

class _{{ pageStateClassName page.name }} extends State<{{ pageClassName page.name }}> {

    final {{ storeClassName page.dataElement.target.name }} targetStore;
    {{# if (validatableFlagNeed page.dataElement) }}
    final bool validatable;
    {{/ if }}

    final {{ pageStoreClassName page.name }} pageStore = {{ pageStoreClassName page.name }}();

    _{{ pageStateClassName page.name }}({@required this.targetStore{{# if (validatableFlagNeed page.dataElement) }}, this.validatable{{/ if }} }) {
        pageStore.targetStore = targetStore;

        final navigation = locator<NavigationState>();
        SchedulerBinding.instance.addPostFrameCallback((_) {
            navigation.setCurrentTitle(AppLocalizations.of(context).lookUpValue(context, {{ pageClassName page.name }}.title){{# if page.dataElement.target.representation }} + ' - ${targetStore.{{ variable page.dataElement.target.representation.name }} }'{{/ if }});
        });
        {{# if (validatableFlagNeed page.dataElement) }}
        if (this.validatable != null) {
            pageStore.validatable = this.validatable;
        }
        {{/ if }}
    }
//END editpagefragment.dart.hbs-----{{ page.name }}------------------------------------------------------------------------------------------
