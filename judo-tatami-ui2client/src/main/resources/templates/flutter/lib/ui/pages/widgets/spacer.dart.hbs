{{ log 'Spacer generation for' (variable application.name) '-' (fqClass page.name)'page' label level='debug' ~}}
{{# if addDebugToTemplate }}//START xmi:id="{{ getXMIID this }}" spacer.dart.hbs-----{{ page.name }}----------------------------------------------------------------------------------------------------{{/ if }}
JudoSpacer(
    col: {{ col }},
    row: {{ row }},
),
{{# if addDebugToTemplate }}//END spacer.dart.hbs-----{{ page.name }}----------------------------------------------------------------------------------------------------{{/ if }}
