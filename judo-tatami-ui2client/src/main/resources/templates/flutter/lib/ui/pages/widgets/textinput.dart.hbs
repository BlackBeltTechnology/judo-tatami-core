{{#if page.isPageTypeView}}
Observer(
    builder: (_) => JudoInputText(
        key: UniqueKey(),
        col: {{ col }},
        icon: Icon(Icons.{{ this.leadingIcon.name }}),
        label: '{{ label }}',
        initialValue: this.{{ variable page.dataElement.name }}Store.{{ attributeType.name }},
        readonly: true,
    )
),
{{/if}}
{{#if page.isPageTypeCreate}}
JudoInputText(
    col: {{ col }},
    icon: Icon(Icons.{{ this.leadingIcon.name }}),
    label: '{{ label }}',
    onChanged: (value) => this.{{ variable page.dataElement.name }}Store.set{{ className attributeType.name }}(value.toString()),
),
{{/if}}
{{#if page.isPageTypeUpdate}}
JudoInputText(
    col: {{ col }},
    icon: Icon(Icons.{{ this.leadingIcon.name }}),
    label: '{{ label }}',
    initialValue: this.{{ variable page.dataElement.name }}Store.{{ attributeType.name }},
    onChanged: (value) => this.{{ variable page.dataElement.name }}Store.set{{ className attributeType.name }}(value.toString()),
),
{{/if}}