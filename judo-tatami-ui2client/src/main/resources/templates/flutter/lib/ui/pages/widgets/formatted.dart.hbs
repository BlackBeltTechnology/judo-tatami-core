{{# if addDebugToTemplate }}//START xmi:id="{{ getXMIID this }}" formatted.dart.hbs-----{{ page.name }}-----------------------------------------------------------------------------------------------{{/ if }}
{{# if column }}
    {{ log 'Formatted text generation for' (variable application.name) '-' (fqClass table.name)'table' (variable dataName) (variable column.attributeType.name) level='debug' ~}}
    {{# if (isNumericType column.attributeType.dataType) }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/numeric.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{ else }}
    {{# if (isEnumType column.attributeType.dataType) }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/enum.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{ else }}
    {{# if (isTimestampType column.attributeType.dataType) }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/timestamp.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{ else }}
    {{# if (isDateType column.attributeType.dataType) }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/date.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{ else }}
    {{# if (isBooleanDataType column.attributeType.dataType) }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/boolean.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{ else }}
        {{> flutter/lib/ui/pages/widgets/formatted/table/other.dart.hbs column=column dataName=dataName page=page attributeType=attributeType }}
    {{/ if }}
    {{/ if }}
    {{/ if }}
    {{/ if }}
    {{/ if }}
{{ else }}
    {{ log 'Formatted text generation for' (variable application.name) '-' (fqClass page.name)'page'(variable page.dataElement.target.name) attributeType.name level='debug' ~}}
    {{> flutter/lib/ui/pages/widgets/formatted/text.dart.hbs column=column dataName=dataName col=col row=row isLabelVisible=isLabelVisible attributeType=attributeType label=label }}
{{/ if }}
{{# if addDebugToTemplate }}//END formatted.dart.hbs-----{{ page.name }}-------------------------------------------------------------------------------------------------{{/ if }}
