{{ log 'Text generation for' (variable application.name) '-' (fqClass page.name)'page' label level='debug' ~}}
{{# if addDebugToTemplate }}//START xmi:id="{{ getXMIID this }}" text.dart.hbs-----{{ page.name }}----------------------------------------------------------------------------------------------------{{/ if }}
JudoText(
    col: {{ col }},
    {{# neq row 1.0 }}row: {{ row }},{{/ neq }}
    text: AppLocalizations.of(context).lookUpValue(context, '{{ text }}'),
),
{{# if addDebugToTemplate }}//END text.dart.hbs-----{{ page.name }}------------------------------------------------------------------------------------------------------{{/ if }}
