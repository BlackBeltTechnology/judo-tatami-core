{{ log 'Label generation for' (variable application.name) '-' (fqClass page.name)'page' label level='debug' ~}}
JudoLabel(
    text: '{{label}}',
{{#if this.leadingIcon }}
    leadingIcon: Icon(Icons.{{ this.leadingIcon.name }}),
{{/if}}
{{#if this.trailingIcon }}
    trailingIcon: Icon(Icons.{{ this.trailingIcon.name }}),
{{/if}}
),
