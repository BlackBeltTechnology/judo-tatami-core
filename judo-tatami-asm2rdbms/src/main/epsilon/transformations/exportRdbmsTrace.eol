
for (tr in transformationTrace.transformations) {
	for (t in tr.targets) {
		if (not t.isTypeOf(Map)) {
			var row : TRACE!Trace = new TRACE!Trace();
			row.asmType = tr.source.eClass;
			row.asmIdentifier = tr.source.id;
			row.rdbmsType = t.eClass;
			row.rdbmsIdentifier = t.id;	
			
			log.debug("ASM " + tr.source.id + ": " + ASM.getElementById(tr.source.id));
			log.debug("RDBMS " + t.id + ": " + RDBMS.getElementById(t.id));
		}
	}
}