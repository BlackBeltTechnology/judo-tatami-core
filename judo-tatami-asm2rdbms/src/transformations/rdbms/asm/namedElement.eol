
@cached
operation ASM!ENamedElement sqlName() : String {
    if (self.hasAnnotation("shortName")) {
	    return self.getAnnotationValue("shortName").toUpperSnakeCase();
    } else {
    	return abbreviate(self.name, shortNameSize()).toUpperCase();
	}
}
